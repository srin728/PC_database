# Paper-link reliability patch

From the `PC_database` repository root, run:

- Windows: `py apply_link_reliability_fix.py`
- macOS/Linux/Codespaces: `python3 apply_link_reliability_fix.py`

Then run the repository tests, `python3 scripts/audit_links.py`, and `python3 scripts/build.py`, commit, and push.
